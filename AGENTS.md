# AGENTS.md - Calculatify 編碼規範

> 本文件為 AI 助手提供在此專案工作時的規範指引。

## 文件管理規範

**所有專案文件統一存放在 `docs/` 資料夾內進行管理。**

根目錄僅保留 `AGENTS.md` 作為 AI 助手配置檔案，其他所有規劃、設計、策略文件均應置於 `docs/` 目錄：

| 文件 | 位置 | 用途 |
|------|------|------|
| `AGENTS.md` | `/` (根目錄) | AI 助手編碼規範與指引 |
| `DESIGN.md` | `docs/` | 網站界面設計與功能安排規範 |
| `PLANS.md` | `docs/` | Phase 0-6 內容規劃與行事曆 |
| `ROADMAP.md` | `docs/` | 產品路線圖 (24個月規劃) |

**原則**：
- 所有策略、規劃、設計文件 → `docs/`
- 技術規範、編碼標準 → `AGENTS.md` (根目錄)

## 溝通語言規範（重要！）

**所有對話、程式碼註解、提交訊息和文件撰寫都必須使用繁體中文。**

- **使用者互動**：使用繁體中文回應所有問題和請求
- **程式碼註解**：使用繁體中文撰寫所有註解
- **Git 提交訊息**：使用繁體中文撰寫提交訊息
- **文件撰寫**：使用繁體中文撰寫所有技術文件
- **變數命名**：程式碼中的變數和函式名稱仍使用英文，但註解必須是繁體中文

## 專案概述

Calculatify 是一個**專門提供計算教學與工具的線上教育平台**，以 **SEO 和 Google AdSense 合規優先** 為核心策略。

- **框架**: Next.js 16 (App Router)
- **語言**: TypeScript 5（嚴格模式）
- **UI**: React 19, Tailwind CSS v4
- **建構工具**: Next.js（開發環境使用 Turbopack）
- **套件管理**: npm
- **目標市場**: 美國（英文內容）
- **核心策略**: Content-First Architecture（內容優先）
- **當前階段**: Phase 0 - AdSense 過審衝刺（6週計畫）

## 網站定位：教育資源平台

### 關鍵區別

**錯誤定位（工具農場）**：
- 首頁全是工具卡片
- 無教育內容
- Google AdSense 審查：拒絕

**正確定位（教育資源）**：
- 首頁 70% 教育文章 + 30% 工具展示
- 每個工具搭配詳細教學內容
- Google AdSense 審查：通過

### Content-First Architecture 原則

1. **首頁版面比例**：
   - 70% 教育文章（Featured Guides）
   - 30% 工具展示（Essential Tools）

2. **工具頁面結構**：
   - 30% 工具介面
   - 70% 教育內容（HowTo、Formula、FAQ）

3. **內容發布頻率**：
   - 每週 3 篇短文章（500-800字）
   - 每 2 週 1 篇深度指南（1500-2000字）
   - 每週 1 個新工具

## 網站風格與設計規範

### 設計哲學：「雜誌封面風格」

想像首頁是 Forbes 或 Wired 的線上版——內容為主，工具為輔。

### 配色方案：Indigo + Slate（專業、現代）

```css
/* 主色調 - 專業、信任 */
primary: '#4F46E5',      /* Indigo-600 */
primary-hover: '#4338CA', /* Indigo-700 */

/* 背景 */
background: '#FFFFFF',   /* 純白 */
surface: '#F8FAFC',      /* Slate-50，用於區塊分隔 */

/* 文字 */
text-primary: '#0F172A',   /* Slate-900，標題 */
text-secondary: '#475569', /* Slate-600，內文 */

/* 強調 */
accent: '#10B981',       /* Emerald-500，成功/計算結果 */
```

### 字體層次規範

```css
/* Hero 標題 */
text-4xl md:text-5xl font-bold tracking-tight text-slate-900

/* 區塊標題 */
text-2xl font-bold text-slate-900 mb-6

/* 文章卡片標題 */
text-lg font-semibold text-slate-900

/* 內文 */
text-slate-600 leading-relaxed
```

### 間距與留白原則

- Section 間距：`py-16 md:py-24`（大面積留白 = 高級感）
- Container：`max-w-7xl mx-auto px-4 sm:px-6 lg:px-8`
- 卡片內距：`p-6` 或 `p-8`（呼吸感）
- 圓角：`rounded-xl` 或 `rounded-2xl`（柔和感）

### 元件設計規範

**卡片**：
- 使用 `bg-white shadow-sm hover:shadow-lg transition-shadow`（微互動）
- 不要使用 border，用陰影創造層次

**按鈕**：
- 主要：`bg-indigo-600 text-white hover:bg-indigo-700`
- 次要：`bg-slate-100 text-slate-900 hover:bg-slate-200`

**圖示**：
- 使用 Heroicons 或 Lucide React
- 風格：簡約線條型（Outline）

## 首頁版面結構

### 1. Hero Section（15% 版面）

**左側（60%）**：
- 大標題："Master Everyday Math"
- 副標："Expert guides and accurate tools for finance, health, and daily life"
- CTA：
  - 主要："Explore Guides"（連到文章區）
  - 次要："Try Calculators"（連到工具區）

**右側（40%）**：
- 簡約插圖（使用 unDraw 或 Heroicons）
- 白底 + Indigo 主色

### 2. Featured Guides（45% 版面）⚠️ 核心區塊

**設計**：3 張大卡片（Grid: `grid-cols-1 md:grid-cols-3`）

每張卡片：
- 圖示（📊 📅 🎂）
- 標題（The Complete Percentage Guide）
- 150字摘要
- "Read Full Guide →" 連結

### 3. Essential Tools（25% 版面）

**標題**："Practice What You Learned"（不要用 "Popular Tools"）

**工具卡片**（小尺寸，5個）：
- 🔢 Percentage Calculator
- 📅 Date Calculator
- 🎂 Age Calculator
- 🎲 Random Number Generator
- 💰 Tip Calculator

### 4. Footer

**必備連結**：About Us | Contact | Privacy Policy | Terms of Service

**版權**：© 2026 Calculatify. Educational resources for everyone.

## 內容策略與架構

### 必要頁面（AdSense 強制要求）

| 頁面 | URL | 字數 | 用途 |
|------|-----|------|------|
| About Us | `/about` | 800字 | 品牌故事、使命宣言 |
| Contact Us | `/contact` | 300字 | 聯絡表單、email |
| Privacy Policy | `/privacy` | - | iubenda 生成 |
| Terms of Service | `/terms` | - | 標準模板 |

### 內容分類

**深度指南**（`/guides/`）：
- 每篇 1500-2000字
- 教育性、權威性內容
- Schema: HowTo + FAQ

**工具教學**（`/tools/`）：
- 每個工具搭配教學內容
- 頁面結構：工具 + HowTo + Formula + FAQ

**部落格文章**（`/blog/`）：
- 每篇 500-800字
- 工具使用教學、實用技巧
- 建立更新頻率

### 6週內容規劃

詳細規劃請參考 `PLANS.md` 文件。

**Week 1-6 目標**：
- 5 個可用工具
- 5 篇深度指南（每篇 1500+ 字）
- 18 篇短文章（每篇 500+ 字）
- 總頁面數量：27 頁

## 建置/檢查指令

```bash
# 開發伺服器（Turbopack）
npm run dev

# 正式環境建置
npm run build

# 啟動正式伺服器
npm start

# 語法檢查
npm run lint              # 檢查所有檔案
npx eslint .              # 替代方案：詳細輸出檢查結果
npx eslint --fix .        # 自動修復語法問題

# 型別檢查
npx tsc --noEmit

# 格式化檢查
npx prettier --check .    # 檢查所有檔案
npx prettier --write .    # 自動格式化所有檔案
```

**注意**: 目前未設定測試框架。建議新增 Jest、Vitest 或 Playwright 進行測試。

## 提交前驗證規範（重要！）

**每次新增功能或提交程式碼前，必須執行以下驗證：**

```bash
# 1. ESLint 語法檢查
npm run lint

# 2. TypeScript 型別檢查
npx tsc --noEmit

# 3. Prettier 格式化檢查
npx prettier --check .
```

**驗證流程要求：**

- **所有檢查必須通過**才能提交
- 若有 ESLint 錯誤，使用 `npx eslint --fix .` 嘗試自動修復
- 若有 Prettier 格式問題，使用 `npx prettier --write .` 自動格式化
- 若 TypeScript 有錯誤，必須手動修正型別問題
- **嚴禁提交帶有錯誤或警告的程式碼**

**本地建置測試（推薦）：**

在推送到 Vercel 前，建議先本地執行建置驗證：

```bash
npm run build
```

確保本地建置成功，再推送到遠端。

## 程式碼風格規範

### SEO 優先原則（重要！）

由於本專案以 SEO 為首要目標，請遵循以下原則：

- **每個計算器都必須有獨立的頁面路由**（如 `/tools/percentage-calculator`）
- **標題（Title）和描述（Description）必須針對搜尋意圖優化**
- **使用 Next.js Metadata API** 設定每頁的 meta 標籤
- **圖片必須有描述性的 alt 文字**
- **URL 結構應簡潔且包含關鍵字**
- **確保網站效能優秀（Core Web Vitals）**
- **使用結構化資料（Schema.org JSON-LD）標記計算器內容**
- **為 AI 搜尋引擎提供清晰的內容結構**
- **每個工具頁面必須包含 1000+ 字的教育內容**

### TypeScript

- 在 tsconfig 中啟用 `strict: true`（已設定）
- 函式參數和回傳值使用明確型別
- 使用 `import type { ... } from '...'` 匯入型別
- 避免使用 `any` 型別；需要時使用 `unknown` 配合型別守衛

### 匯入規則

```typescript
// 順序：外部函式庫 → 內部模組 → 型別 → 樣式
import type { Metadata } from 'next';
import { Geist } from 'next/font/google';
import Image from 'next/image';
import '@/components/ui';
import './globals.css';
```

- 使用路徑別名 `@/*` 匯入專案根目錄檔案
- 工具函式使用具名匯入，元件/頁面使用預設匯入

### React 元件

```typescript
// 使用箭頭函式定義元件
export default function Home() {
  return <div>...</div>;
}

// 明確定義 props 型別
interface ButtonProps {
  label: string;
  onClick: () => void;
}

export function Button({ label, onClick }: ButtonProps) {
  return <button onClick={onClick}>{label}</button>;
}
```

- 頁面元件使用預設匯出（Next.js 慣例）
- 可重複使用的 UI 元件和工具函式使用具名匯出
- 頂層元件優先使用 `function` 宣告
- 僅在使用用戶端功能時才加入 `use client` 指令

### 命名慣例

- **檔案**: 元件使用 PascalCase（`Button.tsx`），工具函式使用 camelCase（`utils.ts`）
- **元件**: PascalCase（`UserProfile`）
- **函式/變數**: camelCase（`handleClick`, `userData`）
- **型別/介面**: PascalCase（`UserData`, `ApiResponse`）
- **常數**: SCREAMING_SNAKE_CASE（真正的常數）
- **路由**: kebab-case（`/percentage-calculator`）

### 格式化

- 不需要分號（專案風格）
- 字串使用單引號
- 2 空格縮排
- 每行最多 100 字元
- 多行物件/陣列使用尾隨逗號

### Tailwind CSS

- 使用 Tailwind v4 語法 `@import "tailwindcss"`
- 優先使用工具類別而非自訂 CSS
- 使用 `@theme inline` 在 CSS 中自訂主題
- 深色模式：使用 `dark:` 前綴類別
- 響應式：使用 `sm:`, `md:`, `lg:` 前綴（行動優先）
- **配色**：遵循 Indigo + Slate 設計系統
- **間距**：使用大面積留白創造高級感

### 錯誤處理

```typescript
// 非同步操作使用 try/catch
async function fetchData() {
  try {
    const res = await fetch('/api/data');
    if (!res.ok) throw new Error('Failed to fetch');
    return await res.json();
  } catch (error) {
    console.error('Error fetching data:', error);
    return null;
  }
}
```

## 檔案結構

```
app/
├── page.tsx                    # 首頁（Content-First 設計）
├── layout.tsx                  # 根佈局（包含全站 SEO 設定）
├── globals.css                 # 全域樣式（Indigo + Slate 配色）
├── about/
│   └── page.tsx               # About Us 頁面（800字）
├── contact/
│   └── page.tsx               # Contact Us 頁面（300字）
├── privacy/
│   └── page.tsx               # Privacy Policy 頁面
├── terms/
│   └── page.tsx               # Terms of Service 頁面
├── guides/                     # 深度指南
│   ├── percentage-calculation-guide/
│   │   └── page.tsx           # 百分比計算完整教學（2000字）
│   ├── date-calculation-guide/
│   │   └── page.tsx           # 日期計算教學（1500字）
│   ├── age-calculation-guide/
│   │   └── page.tsx           # 年齡計算教學（1800字）
│   ├── random-number-science/
│   │   └── page.tsx           # 隨機數科學（2000字）
│   └── financial-literacy-basics/
│       └── page.tsx           # 財務基礎（1500字）
├── tools/                      # 工具頁面（每個都包含教學內容）
│   ├── percentage-calculator/
│   │   └── page.tsx           # 百分比計算機
│   ├── date-calculator/
│   │   └── page.tsx           # 日期計算機
│   ├── age-calculator/
│   │   └── page.tsx           # 年齡計算機
│   ├── random-number-generator/
│   │   └── page.tsx           # 隨機數產生器
│   └── tip-calculator/
│       └── page.tsx           # 小費計算機
├── blog/                       # 部落格文章（500-800字）
│   └── [article-slug]/
│       └── page.tsx
├── components/                 # 可重複使用元件
│   ├── layout/                 # 布局元件
│   │   ├── Header.tsx         # 頂部導航欄
│   │   └── Footer.tsx         # 頁尾
│   ├── sections/               # 頁面區域元件
│   │   ├── HeroSection.tsx    # 首頁 Hero 區塊
│   │   ├── FeaturedGuides.tsx # 精選指南區塊
│   │   └── EssentialTools.tsx # 工具展示區塊
│   └── cards/                  # 卡片元件
│       ├── GuideCard.tsx      # 指南卡片
│       └── ToolCard.tsx       # 工具卡片
├── lib/                        # 工具函式和輔助程式
│   └── utils.ts
├── types/                      # TypeScript 型別定義
│   └── index.ts
└── metadata/                   # SEO 相關設定和模板
    └── seo-templates.ts
components/ui/                  # Shadcn UI 元件庫
├── button.tsx
├── card.tsx
├── input.tsx
├── select.tsx
└── ...
public/                         # 靜態資源
├── images/
└── icons/
docs/                           # 專案文件
├── DESIGN.md                  # 網站界面設計與功能安排規範
├── PLANS.md                   # 6週內容規劃（Phase 0）
└── ROADMAP.md                 # 產品路線圖
```

## 環境變數

- 用戶端變數前綴為 `NEXT_PUBLIC_`
- 絕不提交 `.env` 檔案
- 使用 `.env.local` 進行本機覆寫

## Git

- 遵循 conventional commit 訊息格式
- 保持提交聚焦且原子化
- 不要提交 `node_modules`、`.next/` 或環境檔案
- **所有提交訊息必須使用繁體中文**

## 重要提醒

### Phase 0 期間禁止事項

- ❌ 宣稱「50+ 工具」、「1M 使用者」等虛假數據
- ❌ 顯示未完成的工具連結（避免 404）
- ❌ 使用中文內容（目標市場為美國）
- ❌ 首頁以工具為主（必須內容優先）

### Phase 0 必須完成

- ✅ About Us、Contact Us、Privacy Policy、Terms of Service 四頁
- ✅ 首頁 70/30 內容比例（文章/工具）
- ✅ 5 個可用工具（Percentage, Date, Age, Random, Tip）
- ✅ 5 篇深度指南（每篇 1500+ 字）
- ✅ 18 篇短文章（每篇 500+ 字）
- ✅ 零 404 錯誤

---

*最後更新：2026-01-30*
*階段：Phase 0 - AdSense 過審衝刺*
